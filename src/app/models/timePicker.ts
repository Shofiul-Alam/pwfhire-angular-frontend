import {CustomTime} from './customTime';

export class TimePicker {
    constructor(
        meridian: boolean = true,
        timeStart:CustomTime = new CustomTime(),
        timeEnd:CustomTime = new CustomTime()
    ){

    }
}